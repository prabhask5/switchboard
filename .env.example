# ──────────────────────────────────────────────
# Switchboard – Environment Variables
# ──────────────────────────────────────────────
# Copy this file to `.env` and fill in your values.
# NEVER commit .env to version control.

# Google OAuth 2.0 credentials
# Create these at https://console.cloud.google.com/apis/credentials
# Authorized redirect URI must be: ${APP_BASE_URL}/auth/callback
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# The public-facing URL of your app (no trailing slash)
# Local development: http://localhost:5173
# Production: https://your-domain.com
APP_BASE_URL=http://localhost:5173

# A random 32+ byte secret encoded as base64, used to encrypt the
# refresh-token cookie with AES-256-GCM.
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
COOKIE_SECRET=CHANGE_ME_generate_with_node_crypto
